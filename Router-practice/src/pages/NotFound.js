import React from 'react';

const NotFound = (props) => {
    return (
        <div className='centered'>
            <p>Page not found!</p>
        </div>
    );
}

export default NotFound;